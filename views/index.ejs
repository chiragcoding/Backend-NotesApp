<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
   <div class="main w-full min-h-screen bg-zinc-900">

    <div class="form p-10" >
        <form action="/create" method="post">
            <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none text-white" type="text" placeholder="Titel goes here" name="title">
            <textarea class="block mt-2 px-5 py-2 bg-zinc-800 resize-none  w-full rounded-md outline-none text-white"  placeholder="Write your task details here"  name="details"></textarea>
            <input class="mt-2 text-white bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task">
        </form>
    </div>
    <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length>0){%>

            <%files.forEach(function(val){%>
                <div class="task w-72  px-3 py-4 rounded-md bg-zinc-900">
                    <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                    <div class="flex flex-wrap w-full justify-between items-center">
                        <a href="/file/<%=val%>" class="text-blue-500 inline-block mt-3">Read More.</a>
                        <a href="/edit/<%=val%>" class="text-zinc-600 mt-3">edit filename</a>
                    </div>
                </div>
           <% })%>

        <% }else{%>
            <h3 class="text-zinc-600">no tasks yet.</h3>
       <% }  %>
    </div>

   </div>
</body>
</html>